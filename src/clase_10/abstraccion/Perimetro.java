package clase_10.abstraccion;

public interface Perimetro {
    //Solo podemos poner metodos abstractos
    //Aunque no es necesario indicar el abstract explicitamente
    //Si es clase publica todos sus metodos son publicos por defecto

    double calcularPerimetro();
    //double calcularArea();
}
